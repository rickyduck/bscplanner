{{#each product in products}}
                      <li>
                        <img {{bind-attr src=product.svg.front}}/>
                        <span>{{product.sku}}</span>
                      </li>
                  
                  {{/each}}