import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:editor/toolbars', 'EditorToolbarsView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
